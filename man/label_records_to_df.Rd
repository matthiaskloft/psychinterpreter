% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_json.R
\name{label_records_to_df}
\alias{label_records_to_df}
\title{Flatten Label Records Into a Two-Column Data Frame}
\usage{
label_records_to_df(records, variable_info)
}
\arguments{
\item{records}{List of label records}

\item{variable_info}{Data frame. Original variables}
}
\value{
Data frame with character columns \code{variable} and \code{label}
}
\description{
Defence in depth rather than a fix for an observed failure: the tiers above
currently guarantee well-formed records, so no LLM response is known to
reach here with a field missing. \code{vapply()} is used anyway because the
previous \code{sapply()} would have degraded silently - returning a list, and so
a list-column - if that guarantee were ever weakened.
}
\details{
Any record that still lacks a usable label falls back to the description
already held in \code{variable_info}, rather than carrying NA into formatting.
}
\keyword{internal}
