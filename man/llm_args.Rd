% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shared_config.R
\name{llm_args}
\alias{llm_args}
\title{Create LLM Configuration}
\usage{
llm_args(
  provider,
  model = NULL,
  system_prompt = NULL,
  params = NULL,
  word_limit = 150,
  interpretation_guidelines = NULL,
  additional_info = NULL,
  echo = "none"
)
}
\arguments{
\item{provider}{Character. LLM provider (e.g., "anthropic", "openai", "ollama")}

\item{model}{Character or NULL. Model name (e.g., "gpt-4o-mini", "claude-3-5-sonnet-20241022")}

\item{system_prompt}{Character or NULL. Custom system prompt to override default (default = NULL)}

\item{params}{List or NULL. ellmer params object (created via ellmer::params()) (default = NULL)}

\item{word_limit}{Integer. Maximum words for LLM interpretations (default = 150)}

\item{interpretation_guidelines}{Character or NULL. Custom interpretation guidelines (default = NULL)}

\item{additional_info}{Character or NULL. Additional context for LLM (default = NULL)}

\item{echo}{Character. Echo level: "none", "output", "all" (default = "none")}
}
\value{
A list with class "llm_args" containing validated LLM settings
}
\description{
Creates a configuration object for LLM interaction settings. Groups all
LLM-related parameters together.
}
\examples{
# Basic config
cfg <- llm_args(provider = "ollama", model = "gpt-oss:20b-cloud")

# Advanced config
cfg <- llm_args(
  provider = "anthropic",
  model = "claude-3-5-sonnet-20241022",
  word_limit = 200,
  params = ellmer::params(temperature = 0.7, seed = 42)
)
}
