% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shared_config.R
\name{fa_args}
\alias{fa_args}
\title{Create Factor Analysis Configuration}
\usage{
fa_args(
  cutoff = 0.3,
  n_emergency = 2,
  hide_low_loadings = FALSE,
  sort_loadings = TRUE
)
}
\arguments{
\item{cutoff}{Numeric. Minimum loading value to consider significant (default = 0.3)}

\item{n_emergency}{Integer. When a factor has no loadings above cutoff, use top N
highest loadings. If 0, factors with no significant loadings are labeled "undefined" (default = 2)}

\item{hide_low_loadings}{Logical. If TRUE, only variables with loadings at or above
cutoff are included in LLM prompt (default = FALSE)}

\item{sort_loadings}{Logical. Sort variables by loading strength within factors (default = TRUE)}
}
\value{
A list with class "fa_args" containing validated FA settings
}
\description{
Creates a configuration object for factor analysis interpretation settings.
This groups FA-specific parameters together for cleaner API usage.
}
\details{
Note: Factor correlation matrices should be passed via \code{fit_results}
parameter in list structure: \code{list(loadings = ..., factor_cor_mat = ...)},
not via \code{fa_args}.
}
\examples{
# Default settings
cfg <- fa_args()

# Custom settings
cfg <- fa_args(
  cutoff = 0.4,
  n_emergency = 3,
  hide_low_loadings = TRUE
)
}
