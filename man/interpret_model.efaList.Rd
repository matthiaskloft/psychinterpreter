% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_interpret_dispatch.R
\name{interpret_model.efaList}
\alias{interpret_model.efaList}
\title{Interpret Results from lavaan::efa()}
\usage{
\method{interpret_model}{efaList}(model, ...)
}
\arguments{
\item{model}{A fitted model from \code{lavaan::efa()} with output="efa"}

\item{...}{Additional arguments including \code{variable_info} (dataframe with
variable names and descriptions) and other arguments passed to interpret_core()}
}
\value{
An \code{fa_interpretation} object
}
\description{
Internal S3 method to interpret exploratory factor analysis results from lavaan's
efa() function when output="efa" is specified.
}
\details{
This method extracts factor loadings using the \code{loadings()} function
from the stats package, which works on efaList objects.
}
\examples{
\dontrun{
library(lavaan)
fit <- efa(data = HolzingerSwineford1939[, 7:15],
           nfactors = 3,
           rotation = "geomin")

var_info <- data.frame(
  variable = paste0("x", 1:9),
  description = paste("Indicator", 1:9)
)

result <- interpret(fit_results = fit, variable_info = var_info,
                    llm_provider = "ollama", llm_model = "gpt-oss:20b-cloud")
}
}
\keyword{internal}
