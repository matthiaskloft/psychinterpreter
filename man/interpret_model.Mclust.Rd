% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_interpret_dispatch.R
\name{interpret_model.Mclust}
\alias{interpret_model.Mclust}
\title{Interpret Results from mclust::Mclust()}
\usage{
\method{interpret_model}{Mclust}(model, ...)
}
\arguments{
\item{model}{A fitted model from \code{mclust::Mclust()}}

\item{...}{Additional arguments including \code{variable_info} (dataframe with
variable names and descriptions, optional for GM) and other arguments passed to interpret_core()}
}
\value{
A \code{gm_interpretation} object
}
\description{
Internal S3 method to interpret Gaussian Mixture Model results from the mclust package.
Automatically extracts cluster means, covariances, and membership probabilities.
}
\details{
This method extracts:
\itemize{
\item Cluster means from \code{model$parameters$mean}
\item Covariance matrices from \code{model$parameters$variance$sigma}
\item Cluster proportions from \code{model$parameters$pro}
\item Membership probabilities from \code{model$z}
}
}
\examples{
\dontrun{
library(mclust)
data <- iris[, 1:4]
gmm_model <- Mclust(data, G = 3)

var_info <- data.frame(
  variable = colnames(data),
  description = c("Sepal length", "Sepal width", "Petal length", "Petal width")
)

result <- interpret(fit_results = gmm_model, variable_info = var_info,
                    llm_provider = "ollama", llm_model = "gpt-oss:20b-cloud")
}
}
\keyword{internal}
