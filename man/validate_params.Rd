% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_parameter_registry.R
\name{validate_params}
\alias{validate_params}
\title{Validate Multiple Parameters}
\usage{
validate_params(param_list, throw_error = TRUE)
}
\arguments{
\item{param_list}{Named list of parameter values to validate}

\item{throw_error}{Logical. If TRUE, throws error on first validation failure. If FALSE, returns results}
}
\value{
If throw_error = FALSE, returns list with:
\itemize{
\item valid: Logical indicating if all validations passed
\item results: Named list of individual validation results
\item invalid_params: Character vector of parameter names that failed validation
If throw_error = TRUE, returns normalized parameter list or throws error
}
}
\description{
Validates a list of parameters using registry validation functions.
Useful for batch validation in config object constructors.
}
\examples{
# Valid parameters
params <- list(word_limit = 150, cutoff = 0.3, verbosity = 2)
validate_params(params, throw_error = FALSE)

# Invalid parameters
params <- list(word_limit = 1000, cutoff = 1.5)
validate_params(params, throw_error = FALSE)
}
