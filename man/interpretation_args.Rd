% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shared_config.R
\name{interpretation_args}
\alias{interpretation_args}
\title{Create Model-Specific Interpretation Configuration}
\usage{
interpretation_args(model_type, ...)
}
\arguments{
\item{model_type}{Character. Type of analysis: "fa" (factor analysis),
"gm" (gaussian mixture), "irt" (item response theory), or "cdm"
(cognitive diagnosis model)}

\item{...}{Model-specific parameters. For FA: cutoff, n_emergency,
hide_low_loadings, sort_loadings. See Details.}
}
\value{
A list with class "interpretation_args" containing validated settings
}
\description{
Creates a configuration object for interpretation settings. The available
parameters depend on the model_type.
}
\details{
\strong{Factor Analysis (model_type = "fa"):}
\itemize{
\item \code{cutoff}: Numeric. Minimum loading value to consider significant (default = 0.3)
\item \code{n_emergency}: Integer. When a factor has no loadings above cutoff, use top N
highest loadings. If 0, factors with no significant loadings are labeled
"undefined" (default = 2)
\item \code{hide_low_loadings}: Logical. If TRUE, only variables with loadings at or above
cutoff are included in LLM prompt (default = FALSE)
\item \code{sort_loadings}: Logical. Sort variables by loading strength within factors
(default = TRUE)
}

\strong{Note:} Factor correlation matrices should be passed via \code{fit_results}
parameter in list structure: \code{list(loadings = ..., factor_cor_mat = ...)},
not via \code{interpretation_args}.
}
\examples{
# Factor analysis with default settings
cfg <- interpretation_args(model_type = "fa")

# Factor analysis with custom settings
cfg <- interpretation_args(
  model_type = "fa",
  cutoff = 0.4,
  n_emergency = 3,
  hide_low_loadings = TRUE
)
}
