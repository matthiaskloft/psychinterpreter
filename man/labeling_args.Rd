% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/labeling_args.R
\name{labeling_args}
\alias{labeling_args}
\title{Create Labeling Arguments Configuration}
\usage{
labeling_args(
  label_type = "short",
  max_words = NULL,
  style_hint = NULL,
  sep = " ",
  case = "original",
  remove_articles = FALSE,
  remove_prepositions = FALSE,
  max_chars = NULL,
  abbreviate = FALSE
)
}
\arguments{
\item{label_type}{Character. Type of labels: "short" (1-3 words), "phrase" (4-7 words),
"acronym" (3-5 chars), or "custom" (default = "short")}

\item{max_words}{Integer or NULL. Exact word count (overrides label_type preset)}

\item{style_hint}{Character or NULL. Style guidance for LLM (e.g., "technical", "simple")}

\item{sep}{Character. Separator between words in final output (default = " ")}

\item{case}{Character. Case transformation: "original", "lower", "upper", "title",
"sentence", "snake", "camel", "constant" (default = "original")}

\item{remove_articles}{Logical. Remove articles (a, an, the) from labels (default = FALSE)}

\item{remove_prepositions}{Logical. Remove prepositions (of, in, at, etc.) (default = FALSE)}

\item{max_chars}{Integer or NULL. Maximum character length for labels}

\item{abbreviate}{Logical. Apply rule-based abbreviation to long words (default = FALSE)}
}
\value{
A labeling_args object (list with class attribute)
}
\description{
Create a configuration object for variable labeling parameters.
This follows the same pattern as interpretation_args() for other model types.
}
\examples{
\dontrun{
# Create configuration for snake case labels
config <- labeling_args(
  label_type = "short",
  case = "snake",
  remove_articles = TRUE
)

# Use with label_variables
labels <- label_variables(
  variable_info,
  labeling_args = config,
  llm_provider = "ollama"
)

# Override specific parameters
labels <- label_variables(
  variable_info,
  labeling_args = config,
  max_words = 2,  # This overrides the config
  llm_provider = "ollama"
)
}

}
