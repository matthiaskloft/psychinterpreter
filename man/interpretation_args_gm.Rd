% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gm_model_data.R
\name{interpretation_args_gm}
\alias{interpretation_args_gm}
\title{Create Interpretation Arguments for Gaussian Mixture Models}
\usage{
interpretation_args_gm(
  analysis_type = "gm",
  n_clusters = NULL,
  covariance_type = NULL,
  min_cluster_size = 5,
  separation_threshold = 0.3,
  profile_variables = NULL,
  weight_by_uncertainty = FALSE,
  plot_type = "auto",
  ...
)
}
\arguments{
\item{analysis_type}{Character string, must be "gm"}

\item{n_clusters}{Integer, number of clusters (optional)}

\item{covariance_type}{Character, covariance structure (e.g., "VVV", "EII")}

\item{min_cluster_size}{Integer, minimum meaningful cluster size (default: 5)}

\item{separation_threshold}{Numeric, overlap threshold for diagnostics (default: 0.3)}

\item{profile_variables}{Character vector, subset of variables to focus on (optional)}

\item{weight_by_uncertainty}{Logical, whether to weight by uncertainty if available (default: FALSE)}

\item{plot_type}{Character, visualization type: "heatmap", "parallel", "radar", or "auto" (default: "auto")}

\item{...}{Additional arguments (currently unused)}
}
\value{
A list of class "interpretation_args_gm" with validated parameters
}
\description{
Creates a configuration object for GM-specific interpretation parameters.
}
\examples{
\dontrun{
# Create GM-specific configuration
gm_args <- interpretation_args(
  analysis_type = "gm",
  min_cluster_size = 10,
  separation_threshold = 0.2,
  plot_type = "heatmap"
)

# Use in interpretation
interpret(
  fit_results = mclust_model,
  variable_info = var_info,
  interpretation_args = gm_args,
  llm_provider = "ollama",
  llm_model = "llama3"
)
}
}
