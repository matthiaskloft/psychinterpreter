% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shared_tokens.R
\name{extract_token_counts}
\alias{extract_token_counts}
\title{Extract token counts from an ellmer token data frame}
\usage{
extract_token_counts(tokens_df, warn = TRUE)
}
\arguments{
\item{tokens_df}{The data frame returned by \code{chat$get_tokens()}, or \code{NULL}.}

\item{warn}{Emit a one-time warning when the schema is unrecognised.}
}
\value{
A list with \code{input_tokens}, \code{output_tokens}, \code{cached_input_tokens}
and \code{schema}. Counts are \code{NA_real_} when the schema is not recognised;
\code{schema} is one of \code{"modern"}, \code{"legacy"}, \code{"none"} or \code{"unknown"}.
}
\description{
Understands both the modern (ellmer >= 0.4) and legacy token schemas.
}
\details{
\code{cached_input} is added to \code{input_tokens}. ellmer documents a turn's tokens
as "input tokens (uncached), output tokens, and input tokens (cached)", so
the two input columns are disjoint parts of one total rather than a total
and a subset. Counting only \code{input} would systematically undercount every
provider that caches prompts.

An empty frame means no assistant turns have completed yet. That is a known
zero, not an unknown quantity, so it reports 0 rather than NA.
}
\keyword{internal}
