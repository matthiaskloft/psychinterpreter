% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gm_utils.R
\name{normalize_gm_shapes}
\alias{normalize_gm_shapes}
\title{Normalize GM Parameter Shapes to a Canonical Form}
\usage{
normalize_gm_shapes(
  means,
  covariances,
  n_variables,
  n_clusters,
  variable_names = NULL,
  cluster_names = NULL
)
}
\arguments{
\item{means}{Numeric matrix, data frame, or vector of cluster means}

\item{covariances}{Numeric 3-d array, matrix, vector, or scalar of covariances}

\item{n_variables}{Integer. Number of variables (p)}

\item{n_clusters}{Integer. Number of clusters (G)}

\item{variable_names}{Character or NULL. Row names to apply}

\item{cluster_names}{Character or NULL. Column names to apply}
}
\value{
List with canonical \code{means} and \code{covariances}
}
\description{
Coerces cluster means and covariances to the canonical shapes every
downstream consumer (diagnostics, prompt building, visualization) assumes:
\code{means[n_variables, n_clusters]} and
\code{covariances[n_variables, n_variables, n_clusters]}.
}
\details{
This is required because \code{mclust} does not use a uniform representation.
For a one-variable mixture it supplies \code{parameters$mean} as a bare vector
of length \code{n_clusters} and \code{parameters$variance$sigma} as a scalar
(equal-variance models) or a vector of length \code{n_clusters} (varying
variance) - neither of which carries the dimensions the rest of the package
indexes into.
}
\keyword{internal}
