% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_interpret_dispatch.R
\name{interpret_model.lavaan}
\alias{interpret_model.lavaan}
\title{Interpret Results from lavaan Models (CFA/SEM)}
\usage{
\method{interpret_model}{lavaan}(model, ...)
}
\arguments{
\item{model}{A fitted model from \code{lavaan::cfa()}, \code{lavaan::sem()},
or \code{lavaan::lavaan()}}

\item{...}{Additional arguments including \code{variable_info} (dataframe with
variable names and descriptions) and other arguments passed to interpret_core()}
}
\value{
An \code{fa_interpretation} object
}
\description{
Internal S3 method to interpret confirmatory factor analysis or structural equation
models from the lavaan package. Automatically extracts standardized factor
loadings and factor correlations.
}
\details{
This method extracts:
\itemize{
\item Standardized factor loadings using \code{lavaan::standardizedSolution()}
\item Factor correlations from latent variable covariances
}

The method filters for measurement model relationships (op == "=~") and
reshapes them into a loadings matrix format.
}
\examples{
\dontrun{
library(lavaan)
model_syntax <- '
  visual  =~ x1 + x2 + x3
  textual =~ x4 + x5 + x6
  speed   =~ x7 + x8 + x9
'
fit <- cfa(model_syntax, data = HolzingerSwineford1939)

var_info <- data.frame(
  variable = paste0("x", 1:9),
  description = paste("Visual/Textual/Speed indicator", 1:9)
)

result <- interpret(fit_results = fit, variable_info = var_info,
                    llm_provider = "ollama",
                    llm_model = "gpt-oss:20b-cloud")
}
}
\keyword{internal}
